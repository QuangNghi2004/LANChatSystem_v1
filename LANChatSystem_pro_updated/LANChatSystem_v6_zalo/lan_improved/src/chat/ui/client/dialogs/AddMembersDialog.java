package chat.ui.client.dialogs;

import javax.swing.*;
import java.awt.*;

/** AddMembersDialog — placeholder, sẽ mở rộng. */
public class AddMembersDialog extends JDialog {
    public AddMembersDialog(Frame parent) {
        super(parent, "AddMembersDialog", true);
        setSize(400, 300); setLocationRelativeTo(parent);
        add(new JLabel("🚧 Đang phát triển: AddMembersDialog", SwingConstants.CENTER));
    }
}
