package chat.ui.client.panels;

import chat.util.Config;
import javax.swing.*;

/** PollPanel — placeholder, sẽ mở rộng. */
public class PollPanel extends JPanel {
    public PollPanel() {
        setBackground(Config.BG_PANEL);
        add(new JLabel("🚧 PollPanel — đang phát triển", SwingConstants.CENTER));
    }
}
