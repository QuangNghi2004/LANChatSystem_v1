package chat.ui.client.panels;

import chat.util.Config;
import javax.swing.*;

/** CloudPanel — placeholder, sẽ mở rộng. */
public class CloudPanel extends JPanel {
    public CloudPanel() {
        setBackground(Config.BG_PANEL);
        add(new JLabel("🚧 CloudPanel — đang phát triển", SwingConstants.CENTER));
    }
}
