package chat.ui.client.components;

import chat.util.Config;
import javax.swing.*;

/** PinnedMessageBar — UI component, sẽ mở rộng. */
public class PinnedMessageBar extends JPanel {
    public PinnedMessageBar() {
        setBackground(Config.BG_PANEL);
    }
}
