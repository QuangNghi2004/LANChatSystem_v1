package chat.ui.client.components;

import chat.util.Config;
import javax.swing.*;

/** MessageInput — UI component, sẽ mở rộng. */
public class MessageInput extends JPanel {
    public MessageInput() {
        setBackground(Config.BG_PANEL);
    }
}
