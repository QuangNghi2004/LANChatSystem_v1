package chat.ui.client.components;

import chat.util.Config;
import javax.swing.*;

/** SearchBar — UI component, sẽ mở rộng. */
public class SearchBar extends JPanel {
    public SearchBar() {
        setBackground(Config.BG_PANEL);
    }
}
