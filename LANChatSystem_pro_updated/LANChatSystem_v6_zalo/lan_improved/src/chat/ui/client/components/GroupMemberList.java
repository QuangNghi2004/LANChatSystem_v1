package chat.ui.client.components;

import chat.util.Config;
import javax.swing.*;

/** GroupMemberList — UI component, sẽ mở rộng. */
public class GroupMemberList extends JPanel {
    public GroupMemberList() {
        setBackground(Config.BG_PANEL);
    }
}
